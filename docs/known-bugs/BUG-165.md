---
id: BUG-165
title: |-
  TsfNative + GJI で cold probe/recovery が続く間に高速打鍵すると、`pending_deferred` の上限(`DEFERRED_QUEUE_CAP=32` VK)超過分が immediate send に落ちて文字が消える(修正済み)
fix_commits: ["61fdd37e"]
related_adr: ["ADR-156", "ADR-193"]
---

# BUG-165: TsfNative + GJI の高速打鍵で defer キューが上限超過し文字が消える

**発見経緯:** 2026-09-25。Zoom チャットで高速タイピングすると崩れる報告の切り分けのため、`ci/e2e-typing-stress` ブランチに高速打鍵E2E(`ts-*` 構成、`typing_stress.rs`)を作り、windows-latest で回したところ検出された。Zoom 本物の症状と同一かは未確認。

**症状:** `ts-tsf-gji-2ms`(TsfNative 相当の RichEdit スーパークラス化入力欄〈ADR-193〉、GJI、注入間隔 2ms)の 1 回(run 36209255447 の run1)で、12試行が全滅し 182 文字が消えた(単打・親指シフト・混在の全種別)。同じ awase.log に `[pending-deferred] count limit exceeded, degrading to immediate send` が 182 件あり、消失文字数と一致。

**再現率:** 同構成の他 8 回(2ms ×7、5ms/3ms 計12回)では再現せず。約 1/8。40〜3ms は全て PASS(41万イベントの注入で `SendInput` の落ち 0、自プロセスの LL フックに全件到達)。cold 状態を作る条件は未特定。

**原因(推測、未検証):** cold probe/recovery の実行中は後続キーが `pending_deferred` に退避される(awase.log で cold probe/recovery 中の defer は確認済み)。probe が長引くと上限 32 VK を超え、超過分が immediate send に落ちて GJI が受け付けず消える。BUG-038 と同じ defer/replay キュー系統(`output/tsf_warmup_coord.rs`、`output/vk_send.rs`)。

**注意:** 2〜3ms は実注入間隔が名目値にならず(遅れの中央値 約1〜1.6ms)、人間の打鍵速度では起きにくいと推測している。Zoom がこの経路かは、実機の journal に `count limit exceeded` が出ているかで判断できる。記録時点で run の artifact を私(記録者)が直接再確認していない(報告値を転記)。

**次の一手:** (a) アイドル後の最初の連打など、cold 状態を意図的に作る構成で再現し閾値を実測する。(b) 実機 Zoom の journal に `count limit exceeded` があるか確認する。修正時は `fix-requires-evidence` により、`ts-tsf-gji-*` 構成か journal replay を回帰の根拠にする。

**修正(2026-09-26、CI未検証):** `DEFERRED_QUEUE_CAP` を 32→2048 に変更し、暴走防止の安全弁として位置づけ直した(導出: cold probe 最大 ~960ms × 500 key/s × 1 モーラ最大 3 VK ≒ 1440 VK に余裕)。
超過時の degrade は文字消失に加えてキュー内の分の追い越しも起こすため、通常の打鍵量では到達しない値にした。
回帰テスト: `output/mod.rs::defer_vk_if_probe_in_flight_keeps_deferring_past_the_old_cap_of_32`。確認: run 36241902201(develop 21d5b421 相当をマージした ci/e2e-typing-stress)で `ts-tsf-gji-2ms` 10 run × 12 試行が全 PASS(fail=0 loss=0)、`count limit exceeded` の出力なし。旧再現率は約 1/8 なので 10 run では確率的な確認に留まる。

## 再現手順(修正前 `61fdd37e^` で。★=今回のセッションで実行して確認済み、無印=未実行(過去 run の報告値の転記を含む))
1. **単体(決定的、Windows ターゲットのみ。`output/` は `cfg(windows)`)★**: `git checkout 61fdd37e^ && git checkout 61fdd37e -- crates/awase-windows/src/output/mod.rs` で回帰テストだけ足し、Windows で `cargo test -p awase-windows --target x86_64-pc-windows-msvc defer_vk_if_probe_in_flight_keeps_deferring` を実行。結果: `32 件目で defer が諦められた`(添字 32 = 33 件目)で失敗(windows-build、run 36261895082、`61fdd37e^`+テストのみ)。修正後は PASS。
2. **CI(確率的、旧再現率 約 1/8。過去 run の報告値で、修正前ビルドでの再実行はしていない)**: `61fdd37e^` を `ci/e2e-typing-stress` にマージし、`e2e-ime.yml` の `ONLY` を `ts-tsf-gji-2ms` にして push(20 run/構成)。期待: 一部の run が FAIL(loss>0)、`awase.log` に `[pending-deferred] count limit exceeded, degrading to immediate send`(消失文字数と同数)。初出は run 36209255447 の run1。
3. **実機**: Windows で `typing_stress.exe --activate-gji --form=tsf --interval=2 --seed=N --layout=layout/nicola_keytop.yab` を起動、`READY-FOR-AWASE` が出たら awase 起動、`=== 完了 ===` まで待つ。`typing_stress.log` の `TYPING_STRESS: verdict=` と `awase.log` の上記警告を見る。
**未特定**: 1/8 を決める cold 状態の条件。人間の打鍵速度での再現(Zoom)。
**修正前ビルドでの再実行(2026-09-26、run 36243021113)**: 上限 32 に戻したビルドで `ts-tsf-gji-2ms` 8 run × 12 試行が全 PASS、`count limit exceeded` なし。**再現せず**(旧 約 1/8 のため 8 run では有意に言えない。CI では再現条件を特定できていない)。
**クローズ(2026-09-26)**: 上限超過の機序は単体テストで決定的に確認・修正済み(`61fdd37e`)。実機/CI での 1/8 再現は修正前ビルドでも再現せず(0/8)、cold 状態の条件は未特定のまま。Zoom で再発したら journal の `count limit exceeded` を確認して再オープンする。
