# 不具合報告（タスクトレイ「不具合を報告」機能）トリアージ台帳

タスクトレイの「不具合を報告」機能（[ADR-095](adr/095-tray-bug-report-cloudflare-intake.md)）が
Cloudflare R2 (`awase-report-bucket`) に保存した個々の報告について、確認済みかどうか・
どの既知バグに該当するか・対応状況を記録する台帳。

`.claude/skills/bug-report-fetch` で report_id を調査する際は、**まずこの台帳に
既存の行があるか確認し**、無ければ調査後にここへ追記する（同じ報告を毎回ゼロから
再調査しないため）。

## 列の意味

- **report_id**: R2オブジェクトキー末尾のULID。
- **reported_at**: 報告送信時刻（UTC、`payload.reported_at`）。
- **症状**: `payload.description` の要約 + symptom_category。
- **原因/該当バグ**: journal・app_log から特定した原因。既存の `known-bugs.md` の
  BUG-N に対応する場合はその番号を書く。
- **対応状況**:
  - `未トリアージ` — まだ調査していない
  - `対応不要` — 実際の不具合ではない（テスト送信・情報不足で再現不可 等）
  - `対応済み(未リリース)` — 修正コミット/PRは存在するが、報告時点の最新リリースには未反映
  - `対応済み(vX.Y.Z〜)` — 該当バージョン以降のリリースに修正が含まれる
  - `未対応` — 原因は特定済みだが修正がまだ存在しない、または修正がdevelop/mainに未マージ
- **メモ**: 修正コミットハッシュ・PR番号・ブランチ名など。

## 台帳

| report_id | reported_at (UTC) | app_version | 症状 | 原因/該当バグ | 対応状況 | メモ |
|---|---|---|---|---|---|---|
| `01M0QPJVRZJTTG7ZZ8S0ZFXHJQ` | 2026-08-23T16:16:54Z | 1.14.0 | description="テスト"（具体症状記載なし）、symptom_category=WrongCharacterOutput | journal上もgive_up/StaleConfirm等の異常パターンなし | 対応不要 | 機能テスト目的の送信とみられる |
| `01M0RE56S6EQ4MTQGJ2EB2W4N0` | 2026-08-23T23:08:34Z | 1.14.0 | 「こういう」と入力→「ういう」（先頭「こ」欠落） | BUG-74: `RawTsfLiteralRecovery` の2連続give-upで文字が痕跡なく消える。app_logに `re-send "ko" scheduled` 直後 `giving up...no re-send` | 対応済み(未リリース) | 修正はPR #97（`fix/bug74-raw-tsf-literal-giveup-drops-char`）でdevelopにマージ済み(2026-08-24 01:37 UTC)。マージがv1.16.0タグ(08-24 00:29 UTC)より後のため v1.16.0 には未反映。次リリースで解消見込み |
| `01M0RN7RR031SFSP8H0RCM9QAH` | 2026-08-24T01:12:22Z | 1.14.0 | 「りよう」と入力→「よう」（先頭「り」欠落） | BUG-74（上記と同一原因）。app_logに `re-send "ri" scheduled` → `giving up` | 対応済み(未リリース) | 上記と同じくPR #97で対応済み・未リリース |
| `01M0S4S6R4C1YJ581YJ9ZGAXXD` | 2026-08-24T05:43:45Z | 1.14.0 | 「つかって」と入力→「っつかって」（先頭が二重化） | BUG-75: `StaleConfirm` 回収が「先頭VKは着弾していない」と無条件仮定して romaji 全体を再送し、着弾済みの子音が二重化する。journalで `verdict: StaleConfirm`、app_logに `backspace ×0 + re-send "tu"` を確認 | 未対応 | PR #103（`45f833d3`）で一度developへマージした suffix 再送方式は、対話設計（Opus 2体・6ラウンド）で致命的欠陥3件（give-up経路での新規文字化け・「先頭VK着弾済み」前提の反例あり・TSFターゲット未検証）が判明しPR #104で revert 済み（2026-08-25）。恒久対策は保留し、PR #105で挙動に影響しない診断専用フィールド（`write_ops_delta`等）をjournalへ追加済み（実機データ収集中）。詳細は `docs/known-bugs.md` BUG-75 追補・追補2参照 |
| `01M0VC3B1NG9JCDWMJTNNK6YAK` | 2026-08-25T02:24:53Z | 1.16.0 | 「無変換キーをawaseが起動していない時は全角カタカナ→半角カタカナ→ひらがなとサイクルするが、awase起動後は無変換キーが完全に無視される」(ThumbKeyMisbehavior)。`muhenkan_solo_tap_always_suppress`をtrue/false変えても無変化と報告。config.tomlで`left_thumb_key = "VK_NONCONVERT"`かつ`keys.ime_on = [..., "VK_NONCONVERT"]`・`keys.engine_off_solo_triple = "VK_NONCONVERT"`を同時設定 | 実装バグではなく設定競合。`Engine::on_input`（`src/engine/engine.rs:336-378`）はPhase1(`check_special_keys`によるホットキーマッチ)→Phase3(`NicolaFsm`の親指キーsolo tap判定)の順で処理し、Phase1がマッチしたら即return（356行目）。`keys.ime_on=["VK_NONCONVERT"]`は無変換の単独タップと無条件マッチする（`matches_key_combo` engine.rs:869-874、親指キー割当てとの排他ガードは`9e879cf`で意図的に撤去済み、engine.rs:919-922のコメント参照）。結果`muhenkan_solo_tap_always_suppress`等が効くPhase3（`resolve_pending_thumb_as_single`, nicola_fsm.rs:1361-1389）に無変換keydownが到達せず、`engine_off_solo_triple`判定(nicola_fsm.rs:1690,1800)も同様に死んでいる。journalの`key_class="LeftThumb"`はhook/journal側のレイアウト分類でDecisionルーティングとは無関係なので矛盾しない | 対応済み(未リリース) | PR #106（`b74fa0ca`）で`engine_off_solo_triple`（同日`engine_off_solo_repeat`へ改称、旧名はalias互換）の既定キーを無変換からVK_INSERTへ変更し、親指キー割当てと独立したソロ連打判定を追加してPhase1ホットキー層との競合を根本回避。追随コミット`9fd70e68`で`on_input`経由では発火しない・KeyUp未到達でラッチ固着する実装バグ2件も修正、developマージ済み（`3772800a`, 2026-08-25）。`src/config.rs`に`left_thumb_key`/`right_thumb_key`との重複設定を警告するバリデーションも実装済み（config.rs:1047）。回帰テスト6件追加。v1.16.0時点では未反映、次リリースで解消見込み。詳細は`docs/adr/055-engine-off-solo-triple.md`追補（2026-08-25）参照 |
| `01M0VGJ2M5KQHD1D9V7HAMBHNT` | 2026-08-25T03:48:38Z | 1.15.0 | 「という」を期待していたのに「rの」となった(WrongCharacterOutput)。Windows Terminal(`windowsterminal.exe`, pid 2848, force_tsf設定)、MS-IME(gji_state=MsImeStrategy)、UWP/InputSite | **journal seq相関で確定（前回セッションの「別InputSiteハンドル間往復」仮説は誤りと判明・訂正）**。実体は Alt+Tab: seq16416/16419 で物理 Alt(vk164)+Tab(vk9) 押下 → explorer.exe(pid=6036、Alt+Tabスイッチャー自体がexplorer.exeホストのUWP InputSite)へ一瞬フォーカスが移り、この間 engine が `EngineActivationSync{target:false}` で正しく deactivate(seq16420)。Alt+Tab解放でWindows Terminal(pid2848)へ復帰(`FocusChanged` seq16430, app_log 03:48:23.893)、`HwndCache: restore [2848 InputSite] ime_on=false (1469ms ago)` でbeliefがOFF復元(これは離脱時点の実状態を正しく反映しており、このrestore自体は誤りではない)。**問題は復帰後の再同期がユーザーの次打鍵頼みだったこと**: 復帰から1230ms後、ユーザーが最初のキー(vk82 'R'物理キー)をdownした瞬間(seq16432, elapsed 48468982)の`decision`は`PassThrough`(=リテラル'r'出力)。その44ms後(seq16436, 48469026)にようやく`ImeOpenApplied{open:true, reason:ImmBrokenForceOn}`が確定しているが、これはR押下自身が誘発した`ConvClassifyCall`(seq16434)経由の反応的トリガーで、独立した周期リフレッシュがFocusChanged後1230ms間一度も走った形跡がない(journalにTimerFired等の再同期試行ログなし。ただし`dropped_timing`=123のtruncationで欠落の可能性は残る)。直後のK+RightThumb(seq16441-16444, thumb-shiftチョード)は正常にConsumeされ「の」を出力→結果「rの」。ユーザーはBackspace×5(seq16452-16457)で訂正後、同じR+K+RightThumbを再送(seq16458-16464)して正しく入力し直している。`apply_force_on_for_imm_broken`(`runtime/mod.rs:696`)のコメントに「TsfNativeはこれを上書きする周期ポーリングが無い」との既知の記述あり(`runtime/mod.rs:723`)。既存の`docs/known-bugs.md` BUG-16(フォーカス遷移settle スキップに再試行がなく belief ON×実IME OFF放置)と同系統の「再同期が復帰直後の最初の打鍵を保護できない」構造だが、BUG-16の3回の追補修正は仮想デスクトップ切替+Blacklistアプリ(Chrome等)の`Decision`/`Effect`経路が対象で、本件はAlt+Tab起因・force_tsf指定のMS-IME(`ImmBrokenForceOn`)経路であり同一の修正でカバーされているか未確認。なお report本文で言及していた「7秒前のstale HwndCache restore」(`FocusChange [2848→6036]`, seq16470, elapsed 48474821)はEnter送信後の**別の**フォーカス喪失イベントで、「rの」誤入力そのものとは無関係(誤入力は既にBackspaceで訂正済みの後に発生)。ただしこの2回目の往復も`HwndImeCache`のキーが`(pid, class_name)`のみでhwnd単位ではない(`focus/hwnd_cache.rs:28`)ため、explorer.exeが複数の異なる実ウィンドウ(hwnd 68162/66234/197194、いずれもpid=6036 class="Windows.UI.Input.InputSite.WindowClass")を同一キャッシュキーとして扱ってしまう副作用は別途実在する(seq16405-16472のFocusTransition群で確認)。`HWND_CACHE_MAX_AGE_MS`=3,600,000ms(1時間)のため経過時間によるstale判定はほぼ機能しない | 対応済み(未リリース) | BUG-77として原因確定。Opus 2体(architect/premortem_reviewer)のpremortem設計レビュー(複数ラウンド)を経て、①フォーカス復帰時にconvを能動的に推論する案はBUG-19再発リスクでNO-GOと判定、②フォーカス復帰後の最初のキーをresync完了までdeferする案を採用。`fix/focus-resync-alt-tab-literal-leak`ブランチで実装済み(`RawKeyEvent::starts_focus_resync()`/`should_run_idle_conv_check`への`is_first_key_after_focus`追加/`focus_resync.rs`新設/`app/mod.rs`・`key_pipeline.rs`・`focus_tracking.rs`・`message_handlers.rs`配線)。純粋関数テスト24件追加(Linux green、計443件)、`cargo xwin check`/`cargo xwin clippy -- -D warnings`でWindows向けコンパイル・lint確認済み。詳細は`docs/known-bugs.md` BUG-77参照。PR #107でdevelopマージ済み(`09bd39aa`, 2026-08-25)。実機検証は次回セッション。(b) HwndImeCacheのhwnd単位キー化は別課題としてBUG-18に記録 |
| `01M0VJEWSEZFFWAV0JFEVPB3D5` | 2026-08-25T04:21:25Z | 1.15.0 | 「awase の状態が不安定で、Nicolaエンジンがオンになったり、オフになったりを繰り返している」(StuckInRomaji)。UWPアプリ、MS-IME、`Windows.UI.Input.InputSite.WindowClass` | 2要因の重なり。(1) `[idle-conv-check] TsfNative: conv=0x00000010 → belief AssumedRomaji→ObservedEisu` がHiragana変換で実際に入力中（romaji=`u`/`ki`/`ku`等が出力されている最中）にも関わらずライブで繰り返し誤検出し、`EngineSync::DirectInput`経由でEngineをdeactivate(`NotRomajiInput`)させる。物理F2/F4での`UserImeOnEisuReset`回復（数秒おきに発生）と本誤検出が交互に起き「オンオフ繰り返し」に見える。(2) 同一UWPアプリ内の2つのInputSiteハンドル間でユーザー操作なしにフォーカスが往復し（`FocusChange [29976→6036]`⇔`[6036→29976]`、間隔約2秒）、そのたびに`HwndCache: restore`→`force-ON (ImmBrokenForceOn)`が発火（journal `ImeOpenApplied` 90件中`ImmBrokenForceOn`30件・`DriftCorrection`25件、うち17件はDriftCorrection(false)から200ms未満でImmBrokenForceOn(true)へ反転するタイトなping-pong）。BUG-18（往復でOffCold残留・文字欠落、GJI限定で修正済み）・BUG-22（stale ObservedEisuキャッシュ復元、修正済み）のいずれも「MS-IME×ライブconv誤読」の本パターンはカバーしない。加えて`is_eligible_for_ime_force_on()`（`state/platform_state.rs:650`）が`effective_open()`（actuationの根拠に使うべきでないとコード自身が明記、ADR-087 §5 Phase3 item17、BUG-63の原因と同型）に依存したまま`issue_open_warrant()`へのPhase 3配線が未完了という構造要因も関与。ログ前半には`[drift] correction: observed=true ≠ desired=false for 659701ms〜755000ms超`と11分以上収束しない別区間もあり、要継続観察 | 未対応 | 新規BUG番号は未割当（次回セッションで原因の切り分け・修正着手）。report取得元: `docs/adr/095-tray-bug-report-cloudflare-intake.md`。**2026-08-25 develop最新(`afd1c9f3`, v1.16.0)で再調査(Opus)、3要因とも未対応のまま残存を確認**: (1) `conv_classify.rs`/`src/engine/conv.rs::from_u32` の `NATIVE==0`→`ObservedEisu`即決は無条件のまま、BUG-57で作られた保護`ConvMode::is_eisu_evidence`(`conv.rs:75`)は本番呼び出し元ゼロで idle-conv-check 経路に未配線。(2) `is_eligible_for_ime_force_on()`(`platform_state.rs:650`)は今も`effective_open()`依存、`issue_open_warrant()`(ADR-087)はshadowモードのみで強制版`into_actuation`は本番未使用とdocに明記。(3) `focus/hwnd_cache.rs`は2026-06-15から無変更でInputSite往復ガード新設なし。**追加判明**: BUG-69由来のforce-ONクールダウン`FORCE_ON_RETRY_COOLDOWN_MS`(`tuning.rs:347`)は`FocusChanged`のたびに`force_on_retry`がリセットされる設計のため、約2秒間隔のフォーカス往復では毎回クールダウンが無効化され一度も抑制が効かない構造的欠陥も発見。**2026-08-25 二体のOpus(architect/premortem_reviewer)によるpremortem設計レビューで上記診断を訂正・上書き**（生のapp.log 1461行を実際に読み直して判明、上記の「conv誤読」「同一UWPアプリ内2ハンドル往復」という原因特定は誤りだったと確定）: ★conv=0x0010は誤読ではなく実際にMS-IMEが半角英数化していた(4経路の独立観測が一致)。idle-conv-checkの`ObservedEisu`判定・Engine停止は正しい挙動だった。★往復の正体は「同一UWPアプリ内2ハンドル」ではなく、pid29976=Windows Terminal(`CASCADIA_HOSTING_WINDOW_CLASS`)・pid6036=別のUWPアプリ・Chrome(17552/19044)という**複数の別アプリ間の通常のフォーカス往復**。誤検出29件中フォーカス変更200ms以内は2件のみで大半は無相関。★真の構造: ①実IMEが半角英数に落ちる→②`ime_mode_fsm`が`[ime-mode] drift detected`で25回検知するが**ADR-094でconv force-write機構を撤去済みのため何も直さない**→③idle-conv-checkが正しくObservedEisuを検出しEngineを止める(正しい判断)→④ユーザーが手動でかなキー(34回)を押して復帰→⑤convは英数のまま→①へ。「オンオフ繰り返し」の実体はこの34回の手動復帰。★最有力の手がかり: `[warrant-shadow] ... would_have_blocked=true`が問題の書き込み23件中**23件全て**に出ており、`issue_open_warrant()`(ADR-087)のshadow判定は既に正しく「授権なし」と判定していた。★HwndCacheが33秒前のstale `ObservedEisu`を復元→直後force-ON→直後に実conv=Eisu化、という順序がEisu化4回中2回で確認され、awase自身のforce-ONが実IMEを英数化している疑いあり(時間的相関のみ、因果未証明)。★調査過程で新たな汚染源(MS-IME環境なのにGJI候補ウィンドウ`GoogleJapaneseInputCandidateWindow`が342回表示)を発見、既知バグのどれにも未該当、別途調査要。★提示した未使用観測手段(`ime_composition_active`/`MSCTFIME UI`)は実測で発火ゼロ件と判明し却下。推奨方針は「W: `issue_open_warrant()`の強制化(ADR-087 Phase3 item15)を主軸、H: HwndCacheのstale復元停止を併走」で、原因1(コンフィグ誤読)を前提にしていたA/B'案は撤回。次のアクションはADR-094撤去理由の確認・warrant強制化の影響範囲確認・gji-obs342件の謎調査・ユーザーへの利用IME構成の確認。詳細な生ログは`/tmp/claude-1001/-home-cuzic-rust-nicola/4f885133-83ab-4c6e-add4-86b8acd5ef48/scratchpad/`(打鍵列を含むため取り扱い注意)。**同日、premortem_reviewerが上記W/H推奨も反証・NO-GO、architectも全面同意し撤回**: ①ログは末尾200KBのみに切り詰められており「23件」「342回」等は窓内の数字に過ぎない。②`[conv-mode]`の実遷移は16分でわずか7回(Kana→Eisu 4回)であり、idle-conv-checkの23回は「23回の誤検出」ではなく同一持続状態の23回再検出。③**Wは無効な提案だった**: `would_have_blocked=true`の23件は全て`open=false`/`strategy: "idle_conv_check_direct_input"`で、Wが対象とする`is_eligible_for_ime_force_on()`はforce-ON(`open=true`側)しかゲートしない。同じ16分でforce-ONは53回発生しているがwarrant-shadow行を1本も伴わない。「23/23件拒否」という数字自体もログ実装の選択バイアス(`ime_controller.rs`の`log_shadow_warrant`が許可時は`debug!`・拒否時のみ`info!`のため分母が記録されていない)。④**Hは既に実装済みでno-op**: `HwndCache: restore ... mode=ObservedEisu`ログは`hwnd_cache.rs:78`のガード適用**前**の生キャッシュ値であり、実belief適用は`platform_state.rs:1073`の`cache_restore_eisu_guard`(2026-07-09 MS Edge同型事故対策、`golden_scenarios.rs:553-590`にテスト済み)を通っている。⑤**新しい本命仮説(未検証)**: Kana→Eisu化4回中3回で「FocusChange→stale`ObservedEisu`キャッシュ復元→force-ON→直後に実conv=Eisu化」という並びが確認され、**MS-IME向けforce-ONが送るキー列自体が「IME-ON かな」ではなく「IME-ON 半角英数」に着地している疑い**(`docs/experiments.md`エントリ01の`VK_DBE_ALPHANUMERIC`誤解と同型パターン)。検証はforce-ON送信箇所にVK列を1行ログ追加するだけで済む。⑥gji-obs 342件は`win_event_obs.rs`のGJI candidate SHOW/HIDEフックがhwnd/pidフィルタ無しでシステム全体を対象にしているためと推定(未検証)。**両エージェントが合意した結論**: これ以上の設計は行わず、まず(a)`log_shadow_warrant`のdebug/info統一、(b)force-ON送信VK列のログ追加(最優先)、(c)GJI candidateイベントのpidログ追加、の3点(挙動を変えない診断ログのみ)を先に実装し、ユーザーに実機で再現・新しいログを取得してもらってから本格設計に入るべき。3ラウンド連続で「実データ確認後に前提が覆った」ため、次回セッションはこの診断ビルドの実装 → ユーザーによる実機再現 → 新ログでの原因確定、から着手すること |

| `01M10VJWF7R8TNZAM08THVZDT7` | 2026-08-27T05:33:45Z | 1.16.1 | `keys.ime_on` に `VK_NONCONVERT` を追加し、無変換単独タップをパススルー設定にしてもMS-IMEへ渡らない。v1.13.0では同設定でカタカナ変換に使えていた(ThumbKeyMisbehavior) | Phase 1ホットキー層が engine 活性中の bare 親指キーを `keys.ime_on=["VK_NONCONVERT"]` として先に consume し、Phase 3 のNICOLA同時打鍵/単独タップ判定へ渡していなかった。journalでも無修飾 `LeftThumb` KeyDown が `decision.kind="Consume"` かつ `ImeEvent::UserImeSetIntent target=true` を繰り返すことを確認 | 対応済み(未リリース) | PR-2で `Engine::is_bare_thumb` を追加し、engine活性中の修飾なし親指キーを手動/自動IMEコンボ全体から除外。`Ctrl+無変換`等の修飾付きIMEコンボは従来どおり維持。回帰テスト T-1/T-2/T-16 を追加 |
| `01M0Z2H9STD17HG66RQ9ERZJ0R` | 2026-08-26T12:56:48Z | 1.16.1 | PowerToys「境界線のないマウス」(Mouse Without Borders) 使用中、ローカル側でawaseが動いていると「英数」「かな」キーが効かず入力文字種を切り替えられない(ThumbKeyMisbehavior)。GJI、`app_kind: Uwp`→実際の症状はMWB中継ウィンドウ(`app_kind: Win32`, journal上`profile: "ImmCross"`＝コード上`AppImeProfile::Standard`)フォーカス中に発生 | BUG-90: journalで中継ウィンドウ(`powertoys.mousewithoutbordershelper.exe`)フォーカス中に実際の親指シフト日本語入力が機能していることを確認(Consume 22件)。「かな」キーはIME ON まで到達するが「英数」キーには対応するIME actuationログがない。GJI稼働中はprofileを問わず英数キーがSuppressされる(ImmCross経路は無条件Suppress、Imm32Unavailable/TsfNative経路はBUG-52対策のis_dbe_mode_key_down条件)という仮説はあるが、旧journalには配送判断(Allow/Suppress)自体が記録されておらず未確定 | クローズ(disable_apps回避策で対応) | 当初disableした`app_overrides.disable_apps`への追加案はOpus敵対的レビューでNO-GO(中継ウィンドウで実際に入力が機能しているため丸ごと無効化はワークフロー破壊、report2ではdisable_apps発火タイミングにキーが来ず効果ゼロ)。config.rs/config.sample.tomlは変更せず、journalにtransport層の最終配送判断を記録する診断フィールド(`JournalEntry::KeyInput::physical`)のみ追加。ユーザー向け回避策として設定画面に「アプリ無効化」タブを新設し`disable_apps`をGUIから編集可能にした(オプトイン、既定リストへの追加ではない)。ユーザーがこのオプトイン登録で十分と判断したため2026-08-26クローズ、根本原因は未確定のまま既知の制約として記録。詳細は`docs/known-bugs.md` BUG-90参照 |
| `01M0Z3JXVCEN2CDEBECM11PRNQ` | 2026-08-26T13:15:09Z | 1.16.1 | 上記の追加報告。ローカル側awaseを止めリモート側のみ起動しても同じく「英数」キーが効かない。「awaseを起動していなければ問題ない」との記述あり | BUG-90（上記と同一）。journal解析の結果、DBEキー到達時のフォーカスはMWB中継ウィンドウではなくexplorer.exe/sakura.exe(Imm32Unavailable/TsfNative経路)であり、report1(ImmCross経路)とはprofileが異なる。ただしどちらもGJI稼働中の英数キー無条件Suppressという単一の仮説で説明できる(仮説であり配送判断自体は旧journalから未確認) | 調査中(原因未確定) | 上記と同じくBUG-90として記録。詳細は`docs/known-bugs.md` BUG-90参照 |
| `01M10SA5K7J4HZ3C5R1BF6K2QK` | 2026-08-27T04:54:04Z | 1.16.1 | awase Settingsで左親指/右親指キーを無変換/変換に選び直すとconfig.tomlの値が漢字表記からVK_NONCONVERT/VK_CONVERTに変わり、無変換/変換キー単独タップのドロップダウンが表示されなくなる | BUG-94: `awase-settings/src/main.rs`のドロップダウン表示条件が漢字表記`"無変換"`/`"変換"`とのリテラル比較のみで、`THUMB_KEY_OPTIONS`ドロップダウン選択時に書き込まれる内部表記`"VK_NONCONVERT"`/`"VK_CONVERT"`を考慮していなかった。実際のキー入力動作は影響を受けない、GUI表示ロジックのみのバグ | 対応済み(未リリース) | GitHub issue #99と同一原因。`is_muhenkan_thumb_key`/`is_henkan_thumb_key`ヘルパーで両表記を受理するよう修正、回帰テスト追加。PR #116（`fix/settings-thumb-key-dropdown-vk-alias`）でdevelopマージ |
| `01M15R86FJW24278GGD3ETS9QX` | 2026-08-29T03:11:11Z | 1.16.1 | GJI、JISキーボード「KB232」使用。symptom_category=WrongCharacterOutput だが本文は不具合報告ではなく寄稿: 「記号がキートップ通り出す設定がなかったので、作成しました。これで解決していますが、可能であれば標準化していただきたい」として独自編集した`layout_yab`を添付。ログ/config/state_snapshotは添付なし（`attach_log`/`attach_config`/`attach_state_snapshot`いずれもfalse） | 実装バグではない。「KB232」は富士通純正キーボード「FMV-KB232」と判明（やまぶきR標準同梱`NICOLA-F.yab`のコメントに機種名が明記されているのを実際にインストーラをダウンロード・展開して確認）。デフォルト同梱`layout/nicola.yab`と添付`layout_yab`を比較すると、NICOLA本家仕様が未定義のまま余っている物理キー位置（数字段12-13列目・A段11-12列目）に、標準JISキーボードのキートップ印字通りの記号（＠［］：￥＾）を割り当てる独自編集だった。紅皿・DvorakJ等の他実装を実際にダウンロードして調査した結果、同じ位置に記号を割り当てる設計は他実装でも採用されている一般的な流儀と判明 | 対応済み(未リリース、新規インストールのみ) | 「標準化してほしい」という機能要望を採用。当初`feat/nicola-layout-literal-symbols`（commit `07a910f7`、developマージ済み）で既存`layout/nicola.yab`の内容を直接書き換えたが、Opus敵対的レビューで「MSIの`NicolaYab`コンポーネントは`NeverOverwrite="yes"`、ZIP版`install.ps1`も`Copy-IfAbsent`のため、**既にawaseを使っているユーザーがアップグレードしても`layout/nicola.yab`は変わらず、報告者本人にも変更が届かない**」という致命的な指摘を受け設計を修正（`fix/nicola-layout-distribution`ブランチ）: `layout/nicola.yab`は元の内容（Backspace/Escape代用）に戻し、記号キートップ通り出力版は新規ファイル`layout/nicola_keytop.yab`として追加、新規インストールの`default_layout`のみこちらを指す。**既存ユーザーがアップグレードしただけでは反映されず、`default_layout`を`"nicola_keytop.yab"`に手動変更する必要がある**（報告者への返信でこの手順を案内すること）。副次的に、富士通純正ハード向け`layout/nicola_f.yab`にも同じキートップ記号（＾／￥／［）が欠落していたバグを発見・修正（README.mdが同機種にこのファイルを推奨しているにもかかわらず記号が出ない状態だった）。かな44キー配置は無変更。回帰テスト追加（キートップ記号アサーション、nicola.yab/nicola_keytop.yab間のかな一致テスト、後/逃再混入防止テスト、WiXインストーラガードテスト）。添付`layout_yab`は`/tmp`スクラッチパスに保存済み（絶対パス等の機微情報なし）。**追記（2026-08-31）**: 「`nicola_keytop.yab`で対応済み」という上記結論は誤りと判明。添付`layout_yab`をセル単位で`nicola_keytop.yab`/`nicola_f.yab`と比較したところ、どちらとも記号配置が一致しない（NICOLA本家仕様で定義済みの「、」の位置自体がQ段11列目からA段11列目へ移動している等、単なる「余っているスロットへの記号追加」ではなく、他の富士通純正モデルとも異なる独自の物理配列）。専用の`layout/nicola_kb232.yab`を新規追加した（`feat/nicola-kb232-layout`ブランチ、report添付の配列をそのまま採用、かな44キー配置がnicola.yabと一致することを回帰テストで確認）。wix/main.wxsにコンポーネント追加、README.md/README.en.mdに案内追記 |
| `01M13EACMQ7D2VETW75N0BTZ9C` | 2026-08-28T05:28:03Z | 1.16.1 | GJI、JISキーボード、UWPアプリ。4件報告(NoResponse)。①変換キー(親指右)単独タップで漢字変換されない(「変換するように設定してある」との申告あり)。②無変換キー(親指左)単独タップで確定されない(「確定するように設定してある」との申告あり)。③「ぶ」を入力すると`b`になる。④(要望)句読点入力時の自動確定(やまぶきのCV4D相当) | ①**原因未確定（有力な手がかりあり）**。当初`right_thumb_key = "VK_SPACE"`により変換(VK_CONVERT)が親指キー未割当のため`henkan_solo_tap_*`(`NicolaFsm::henkan_vk`経由)が無効化されていることを原因と断定したが、レビュー指摘で撤回: (a) `right_thumb_key = "VK_SPACE"`は人間工学上正当な設定でありユーザーの申告を「設定ミス」と決めつけるべきでない、(b) awase-settingsのGUIは親指キーが変換/無変換以外だと「変換キー単独タップ」設定セクション自体を非表示にする(`is_henkan_thumb_key`、BUG-94)ため、この報告者はそもそもそのGUIで「設定」できなかったはずで申告内容と整合しない、(c) reportのjournalは起動直後7.8秒(60件、FocusTransition/GjiFsmTransition中心)のみで実際の変換キー押下イベントを含まず未検証だった。ユーザーから「'変換キー=右親指キー'という前提がコード全体に埋め込まれているのでは」との再指摘を受け再調査(2026-08-28)、`VK_CONVERT`/`VK_NONCONVERT`に触れる全箇所(`hook.rs::classify_key`/`vk.rs::ImeKeyKind::from_vk`/`engine.rs::SpecialKeyCombos::match_event`/`key_pipeline.rs::kp_stage_shadow_ime_toggle`/`bootstrap.rs`の`henkan_vk`/`muhenkan_vk`導出)を洗い出したが、いずれも`left_thumb_key`/`right_thumb_key`の実際の設定値ベースで条件分岐しており無条件ハードコードは無し(`right_thumb_key="VK_SPACE"`ならVK_CONVERTは`KeyClassification::Passthrough`として素通しされる設計)。**代わりに有力な代替説明を発見**: 出力側(`crates/awase-windows/src/tsf/warmup/probe_fsm.rs:131::decide_transmit_plan`)のコメントに「unicodeはGJI compositionをバイパスし"nお"raceが起きる」と明記されており、一定条件(`nc_confirmed=true`かつ非TSFモード等)で確定ひらがなが`output/vk_send.rs`経由でUnicode直接`SendInput`される("eager path")。この経路ではGJI側に変換候補として保持中の未確定文字列が存在せず確定済みテキストとして着地するため、直後の生VK_CONVERT押下には変換対象が無く何も起きない——awase側のVK_CONVERT処理自体のバグではなく既存のflicker回避トレードオフの副作用の可能性。次の切り分けは実機ログの`[tsf-transmit] ... eager=true/false`(`vk_send.rs:41-52`)確認が最短——ただし**この仮説はこのreportのログでは検証不能と判明**: `app_log_excerpt`を精査したところ、report送信直前の約14秒間(`05:27:49〜05:28:03Z`)には実際の打鍵(`[key-output]`)が1件も記録されておらず(起動→GJI検出→専用Fnキー(F21)ポップアップ対応→即座にトレイの不具合報告を開く、という流れ)、症状1の再現の瞬間はログの記録範囲外だった。ログ内には別時間帯(`01:41〜01:48Z`)に実際のローマ字入力152件があるが、これはMS-IME+Chromeの文脈でreport本文が指すGJI/UWPアプリの状況とは別物。副次的に、直前に見つかった`[gji-charset-write]`/`[gji-charset-popup]`(config1.dbへの専用Fnキー(F21)書き込み)はコード調査の結果**症状1と無関係と確定**(書き込み対象はF21×`SwitchKanaType`=かな種別切替のみで、GJI内部の「変換」機能=VK_CONVERTの割当には一切触れない。トリガ条件は無変換キー側=症状2の別機能でユーザーが明示的に`muhenkan_solo_tap_always_suppress=false`にしポップアップに同意した場合のみ)。詳細は`docs/known-bugs.md` BUG-95「症状1について: 撤回した仮説」「2026-08-28 追加調査」「2026-08-28 さらに追加調査」参照。②は①とは別原因: `keys.ime_on = ["VK_NONCONVERT"]`(無modifier)と`left_thumb_key = "無変換"`が衝突し、Phase1ホットキー層(`check_special_keys`)がPhase3(`NicolaFsm`の親指キー処理)より先に無条件マッチしていた。これはreport `01M10VJWF7R8TNZAM08THVZDT7`と全く同一の原因(`Engine::is_bare_thumb`未実装状態)。③ユーザー独自編集の`layout_yab`右親指シフト面V位置に`ｂ'ｕ`という誤字(正しくは`ｂｕ`)。デフォルト同梱`layout/nicola.yab`は正しく`ｂｕ`。`YabValue::parse`はクォート未対応の任意文字列を無警告でLiteralとして受理してしまう検証不足が一因。**(2026-08-31 BUG-104調査で要再検証)** 同じreportのapp_logに`レイアウト読込失敗: ...NICOLA＋確定.yab: stream did not contain valid UTF-8`という起動ログが2回あり、`ｂ'ｕ`誤字を含む当該ファイル自体がUTF-8デコードで読込に失敗し、実際に有効化されていたのはソート順先頭のバンドル版`nicola.yab`(ｂｕ対応は正しい)だった可能性が高い。③の真因は未確定に戻す必要がある(詳細`docs/known-bugs.md` BUG-104参照)。④はソース内に該当実装なし、未実装機能と確認。**⑤(2026-08-31新規発見)**: `default_layout`に指定した`NICOLA＋確定.yab`がUTF-8でなく読込失敗した際、`LayoutEntry::resolve_index`(runtime/mod.rs)がソート順先頭のバンドル版へ無言でフォールバックしていた。通知はトレイバルーン「1件の警告があります」のみで、ユーザーは自分の独自レイアウトが丸ごと無視されていることに気づいていなかった | 未対応(①、原因未確定・添付ログに再現の瞬間が写っておらず新規報告待ち) / 対応済み(未リリース)(②) / **要再検証**(③、BUG-104参照) / 対応済み(未リリース、develop未マージ)(⑤) | ②はreport `01M10VJWF7R8TNZAM08THVZDT7`と同じくPR #114(`is_bare_thumb`/`suppress_ime_combos`、develop `c478e539`直前にマージ、BUG-93と同一PRだが`is_bare_thumb`部分はBUG番号未採番)で既に修正済み・未リリース。③はBUG-95として新規記録し`src/yab/mod.rs`に`YabValue::lint_raw_cell`/`yab::lint`(クォート未対応セルを検出し行番号付きで警告、`awase-settings`のレイアウト読込/保存時に`layout_status`へ付記)を追加。①向けに追加した`AppConfig::validate_solo_tap_reachability`警告はユーザーレビューで根拠不十分と判明し撤回・削除済み(詳細は`docs/known-bugs.md` BUG-95の「症状1について: 撤回した仮説」参照)。回帰テスト7件追加、`cargo test --lib -p awase`846件green、`cargo check --target x86_64-pc-windows-msvc -p awase -p awase-windows -p awase-settings`確認済み。④はGitHub issue #118として別途起票（機能要望のため台帳外で追跡）。PR #117（`fix/bug95-config-validation-warnings`）、develop未マージ。`/code-review`で`yab::lint()`がレイアウト名行を誤検知するバグ(1行目のクォート含みレイアウト名を誤警告)を指摘され修正済み（commit `f1f17a2b`）。①の追加調査結果はcommit（本行更新分）で記録。**⑤はBUG-104として新規記録**（`docs/known-bugs.md`参照）。`LayoutEntry::resolve_index`のフォールバック先を`nicola_keytop`優先に変更し、フォールバック発生時はモーダルダイアログ(`bootstrap::warn_layout_fallback`)で明示、起動時は設定画面を自動起動するよう修正。`fix/bug104-layout-load-notify`ブランチ（`/home/cuzic/rust-nicola-worktrees/bug95-layout-load-notify`）で実装済み・未コミット、develop未マージ。回帰テスト1件追加、`cargo check --target x86_64-pc-windows-msvc`/`cargo clippy`/`architecture_guard`/`layer_boundary_guard`確認済み。Windows実機での動作確認は未実施 |
| `01M1EFYKJZRMACMP0S8QJRA6T1` | 2026-09-01T12:40:34Z | 1.17.0 | 「境界線のないマウス」(PowerToys Mouse Without Borders)経由でリモートPCを操作中、リモート側でCtrl+無変換のIME OFFが効かない(NoResponse)。ユーザー自身がローカル側・リモート側それぞれからペアで報告 | **原因特定（訂正版。初版は`PHYSICAL_KEY_STATE`/Ctrl検出説明としていたが誤りだった）**。vk 0xF0/0xF2は`VK_DBE_ALPHANUMERIC`(英数)/`VK_DBE_HIRAGANA`(かな)そのもの（Ctrlコンボの内部表現ではない）。`crates/awase-windows/src/runtime/key_pipeline.rs:865`のBUG-14対策（2026-07-06、MS-IME/CTF自身が送る自己注入`VK_DBE_HIRAGANA`をユーザー意図と誤読しIME OFFがEngine ONで上書きされ続けた事故の修正）が、`event.injected==true`のVK_DBE_ALPHANUMERIC/HIRAGANAイベントを無条件で`UserImeSetIntent`への昇格対象から除外している。Mouse Without Bordersはリモート側マシンへ**全てのキー入力をSendInput注入として配送する**（journal上`KeyInput.injected`は102件中102件が`true`）ため、正規のユーザー操作（英数/かなキー押下）もこの除外条件に該当してしまう。リモート側app.logに`[shadow-toggle] injected IMEキー vk=0xF0/0xF2 はユーザー意図に昇格させない (BUG-14)`が多数出力されていることで確定（実機で物理直接操作していた12:30台には`intent 昇格...injected=false`で成功しているログもあり対比可能） | 未対応（原因特定済み、恒久対応は設計判断を要する） | 対になるローカル側report `01M1EG3JYGPQ3RZD24GFP429B4`参照（そちらはローカル側の`disable_apps`が正しく無効化していることの確認用）。重複送信 `01M1EFYZRX7SK6T947V9V95GR1`（同一内容、同一reported_at）。BUG-14対策は「自己注入イベントをユーザー意図と誤読しない」ための意図的な安全策であり、Mouse Without Borders等の正規のリモート転送ツールと、IME自身の自己注入イベントを`injected`フラグだけでは区別できないという構造的トレードオフ。単純に`injected`条件を外すとBUG-14が再発するため、恒久対応は次回セッションでの設計要検討事項として記録のみ行う |
| `01M1EG3JYGPQ3RZD24GFP429B4` | 2026-09-01T12:45:26Z | 1.17.0 | 上記と同一文言だが「この報告はローカル側」 | ローカル側は`config.toml`の`disable_apps`に`powertoys.mousewithoutbordershelper.exe`/`mstsc.exe`が既に登録済み（BUG-90の回避策）。journal上、その2プロセスへフォーカスが移っていた5区間（合計約6分）にKeyInputは0件（正しく無効化）で、sakura.exe操作時の英数/かなキー(VK_DBE_ALPHANUMERIC/HIRAGANA、非injected)は3195695ms〜3197605msで両方とも正常に`PhysicalImeKey`として成功している。ローカル側単体では症状は再現せず、原因は対になるリモート側reportで特定した | 対応不要（診断用の対照報告。実際の原因は上記reportで特定） | 上記参照 |
| `01M1DQ2XHNJHMP43XCWZ4AAY3Z` | 2026-09-01T05:22:45Z | 1.17.0 | Teams(TeamsWebView/WebView2)で親指入力ができなくなる(WrongCharacterOutput)。「[a]を押すと「な」」「[s]を入力すると「とに」」。他アプリでは正常 | **原因未確定**。journal上、該当タイミングの`[a]`/`[s]`押下は親指キー(無変換/変換)関与のない単独`Char`キー入力(`PendingChar`)として処理され、app_logの`[key-output]`は`romaji="u"`/`romaji="si"`を記録——これは添付`layout_yab`の`[ローマ字シフト無し]`面ホームポジション(A位置=`ｕ`、S位置=`ｓｉ`)とNICOLA公式仕様に完全に一致する、awase側から見て正しい出力。フォーカスはms-teams.exe(TeamsWebView)→msedgewebview2.exe(`Chrome_WidgetWin_1`、WebView2の実体)へ600ms以上前に安定済みで、フォーカス競合の直接証拠はjournal内に無い。ユーザーの体感症状(な/とに)とawase側の送信内容(う/し)が食い違っており、Teams側(WebView2+MS-IME)での受信・合成表示の過程で何かが起きている可能性が高いが本journalでは特定できない。なお`config.toml`の`keys.ime_on=["Ctrl+変換","VK_NONCONVERT"]`は無変換キーを修飾キーなしでも登録しており、起動時に`[bug-report] config.toml validation warning: keys.ime_on に親指キー（無変換）が修飾キーなしで設定されています...`という警告が出ていた（v1.17.0の`is_bare_thumb`ガードによりIME ON中はこのコンボ自体発火しない設計で無害だが、ユーザーは警告確認方法が分からず気づいていない。今回のa/s単独キー症状には無関係、journal上も無変換/変換キーの関与なし） | 未対応（原因未確定、追加の再現ログが必要） | 副次的に「トレイの『1件の警告があります』通知に、内容を確認する導線が無い」というUX上のギャップも判明（別途改善候補） |
| `01M1E770WASD46JZ049RC86HFS` | 2026-09-01T10:05:12Z | 1.17.0 | MS-IME使用時、「直接入力モードを使用しない」のチェックを外していると、かな入力中に「英数」キー(VK_DBE_ALPHANUMERIC)を押すと入力していた文字が全て消える(CharacterDropped) | **仮説段階（確定的な再現ログ無し）**。journal上、英数キー(vk=0xF0)押下は`injected=false`かつ`PhysicalImeKey`経由で正常に`UserImeSetIntent{target:false}`→IME OFFとして処理されており、awase側の送信内容そのものに異常は見られない（該当箇所を複数回確認、いずれも正常なIME OFFシーケンス）。ただし本journal内に実際の「複数文字が一括消去される瞬間」（例: 連続backspace等の痕跡）は見当たらず、症状の直接的な再現は捕捉できなかった。仮説: MS-IMEの「直接入力モードを使用しない」設定がOFF（=直接入力モード使用を許可）の場合、IME OFF信号を composition 途中で受け取ったMS-IME自身が、未確定文字列を確定せず破棄する挙動を取る可能性がある（GJIと異なりMS-IME向けにはawase側でcomposition状態を`GjiFsmTransition`のような形で追跡していないため、確認する術が無い） | 未対応（仮説段階、確定的な再現ログが必要） | 報告本文に報告者の個人情報（氏名・メールアドレス）が含まれていたため、この台帳・関連するIssue等では非公開のまま扱うこと |

## 既知バグとの対応関係（この台帳から見つかったもの）

- BUG-74 → report `01M0RE56S6EQ4MTQGJ2EB2W4N0`, `01M0RN7RR031SFSP8H0RCM9QAH`
- BUG-75 → report `01M0S4S6R4C1YJ581YJ9ZGAXXD`
- BUG-18/BUG-22（関連あるが未網羅） → report `01M0VJEWSEZFFWAV0JFEVPB3D5`
- BUG番号なし（`docs/adr/055-engine-off-solo-triple.md`追補で対応記録） → report `01M0VC3B1NG9JCDWMJTNNK6YAK`
- BUG-90（クローズ、disable_apps回避策で対応） → report `01M0Z2H9STD17HG66RQ9ERZJ0R`, `01M0Z3JXVCEN2CDEBECM11PRNQ`
- BUG-94（GitHub issue #99と同一） → report `01M10SA5K7J4HZ3C5R1BF6K2QK`
- BUG-95（`.yab`クォート崩れリテラルのみ、症状1の原因は未確定のまま撤回、症状2の原因もBUG-104調査で要再検証）+ PR #114の`is_bare_thumb`（BUG番号なし部分） → report `01M13EACMQ7D2VETW75N0BTZ9C`（後者はreport `01M10VJWF7R8TNZAM08THVZDT7`と同一原因）
- BUG-104（独自`.yab`がUTF-8でないと起動時に無言でバンドル版へフォールバック、対応済み・未リリース） → report `01M13EACMQ7D2VETW75N0BTZ9C`
- BUG番号なし（配列ファイル寄稿+標準化要望を採用、`layout/nicola_keytop.yab`新設・新規インストール既定化。既存ユーザーは手動オプトインが必要） → report `01M15R86FJW24278GGD3ETS9QX`
- BUG番号なし（2026-08-31追記: `nicola_keytop.yab`では実は未カバーと判明、専用`layout/nicola_kb232.yab`を新規追加） → report `01M15R86FJW24278GGD3ETS9QX`
- BUG番号なし（BUG-14対策の`injected`除外により、Mouse Without Borders等のSendInput注入経由では英数/かなキー(VK_DBE_ALPHANUMERIC/HIRAGANA)によるIME ON/OFFが発火しない。未対応・設計要検討） → report `01M1EFYKJZRMACMP0S8QJRA6T1`（対になる診断用report `01M1EG3JYGPQ3RZD24GFP429B4`）
