[alias]
# 循環的複雑度を含む全 lint チェック（CI と同じ条件）
cc = "clippy --lib -- -A clippy::cargo_common_metadata -D warnings -W clippy::cognitive_complexity"
# Windows GNU クロスコンパイル向け warning 自動修正（cargo win-fix）
win-fix = "fix --target x86_64-pc-windows-gnu --allow-dirty --lib -p awase-windows --tests"
