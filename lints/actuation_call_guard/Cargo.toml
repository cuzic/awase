[workspace]

[package]
name = "actuation_call_guard"
version = "0.1.0"
authors = ["authors go here"]
description = "Flags calls to a designated actuation choke point from outside its declared allow-list of call sites"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
dylint_linting = "6.0.0"

[dev-dependencies]
dylint_testing = "6.0.0"

[package.metadata.rust-analyzer]
rustc_private = true
